# 條款 65 別阻斷計算工作上的事件佇列

以事件為基礎的編程方式會帶來一些限制。但為了這兩個目的:

- 在客戶端應用程式中保留高度的互動性。
- 在伺服器端應用程式中確保所有傳入的請求都能適當地被服務到。

盡可能讓事件迴圈的每個回合保持簡短。

有兩種常見的解決方案。

1. 透過 Worker 將同步的動作變成非同步的動作，形成 non-blocking
2. 將同步的演算法，拆成非同步的小步驟，透過 event loop 形成 non-blocking

